services:
  - type: web
    name: luxe-backend
    env: node
    rootDir: .
    buildCommand: yarn install
    startCommand: bash start.sh
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
